# 🚀 DXF 도면 사진 삽입 앱 - 설치 가이드

## ❗ CDN 차단 문제 해결: npm으로 로컬 설치

네트워크 환경에서 CDN이 차단되는 경우, 라이브러리를 로컬에 설치하여 사용합니다.

---

## 📋 사전 준비

### 1. Node.js 설치 (없는 경우)

**다운로드:** https://nodejs.org/

**확인:**
```bash
node --version
npm --version
```

---

## 🔧 설치 단계

### 1단계: 프로젝트 폴더로 이동

```bash
cd "C:\Users\ppopp\Desktop\리습작업\도로조사\1110"
```

### 2단계: npm 패키지 설치

```bash
npm install
```

**실행 결과:**
```
added 2 packages
node_modules/
├── dxf-parser@1.2.1
└── jszip@3.10.1
```

### 3단계: 라이브러리 파일 복사

```bash
npm run copy-libs
```

**실행 결과:**
```
📦 라이브러리 복사 시작...

✅ 복사 완료: dxf-parser.min.js
✅ 복사 완료: jszip.min.js

✅ 모든 라이브러리 복사 완료!
```

**생성된 폴더 구조:**
```
프로젝트/
├── libs/                      ← 새로 생성!
│   ├── dxf-parser.min.js     ← CDN 대신 사용
│   └── jszip.min.js          ← CDN 대신 사용
├── node_modules/             ← .gitignore에서 제외
├── index.html
├── app.js
├── package.json
└── ...
```

### 4단계: GitHub에 커밋

```bash
git add libs/ package.json copy-libs.js .gitignore
git commit -m "라이브러리 로컬 설치 및 libs 폴더 추가"
git push origin main
```

⚠️ **중요:** `libs/` 폴더를 반드시 커밋해야 합니다!

---

## ✅ 확인

### 로컬에서 테스트

```bash
# 간단한 HTTP 서버 실행
python -m http.server 8000

# 또는
npx http-server -p 8000
```

브라우저에서 `http://localhost:8000` 열기

**콘솔 확인 (F12):**
```
📦 라이브러리 로드 시작...

📦 DxfParser 로드 시도...
   시도: libs/dxf-parser.min.js
   ✅ DxfParser 로드 성공!

📦 JSZip 로드 시도...
   시도: libs/jszip.min.js
   ✅ JSZip 로드 성공!

✅ 모든 라이브러리 로드 완료!
```

### GitHub Pages에서 확인

1. **GitHub 저장소 페이지**로 이동
2. **libs 폴더**가 있는지 확인
3. **웹사이트 열기**
4. **콘솔에서 로드 확인**

---

## 🎯 작동 원리

### 로드 순서:

```
1. libs/dxf-parser.min.js (로컬 파일)
   ↓ 실패하면
2. unpkg CDN
   ↓ 실패하면
3. jsdelivr CDN
   ↓ 실패하면
4. 오류 메시지
```

**장점:**
- ✅ 네트워크 없어도 작동 (로컬 파일)
- ✅ CDN 차단 문제 해결
- ✅ 로딩 속도 향상
- ✅ 안정적인 서비스

---

## ⚠️ iOS 제약사항

### Q: iOS에서 npm을 사용할 수 있나요?

**A: iOS 자체에는 제약이 없습니다!**

npm은 **개발 환경**에서만 사용합니다:
- ✅ PC/Mac에서 npm 실행
- ✅ libs 폴더 생성
- ✅ GitHub에 업로드
- ✅ iPhone에서는 웹사이트만 열면 됨!

**iPhone은 그냥 웹 브라우저로 접속하면 끝!**

---

## 🔄 업데이트 방법

### 라이브러리 버전 업데이트:

```bash
# 1. 새 버전 설치
npm install dxf-parser@latest jszip@latest

# 2. 파일 다시 복사
npm run copy-libs

# 3. GitHub 커밋
git add libs/
git commit -m "라이브러리 업데이트"
git push
```

---

## 🐛 문제 해결

### 문제 1: "npm을 찾을 수 없습니다"

**원인:** Node.js 미설치

**해결:**
```
1. https://nodejs.org/ 에서 Node.js 다운로드
2. 설치 후 터미널 재시작
3. node --version 으로 확인
```

### 문제 2: "모듈을 찾을 수 없습니다"

**원인:** npm install 미실행

**해결:**
```bash
npm install
npm run copy-libs
```

### 문제 3: "libs 폴더가 없습니다"

**원인:** copy-libs 스크립트 미실행

**해결:**
```bash
npm run copy-libs
```

### 문제 4: "GitHub Pages에서 404 오류"

**원인:** libs 폴더가 Git에 커밋 안됨

**해결:**
```bash
git add libs/
git commit -m "libs 폴더 추가"
git push origin main
```

**확인:**
- GitHub 저장소에서 libs 폴더가 보이는지 확인
- 파일 2개 있어야 함: dxf-parser.min.js, jszip.min.js

### 문제 5: "여전히 라이브러리 로드 실패"

**원인:** 파일 경로 또는 권한 문제

**해결:**
```bash
# 파일 존재 확인
ls libs/

# 다시 복사
npm run copy-libs

# 권한 확인 (Mac/Linux)
chmod 644 libs/*.js
```

---

## 💡 대안: 수동 다운로드

npm 없이 수동으로 다운로드할 수도 있습니다:

### 1. libs 폴더 생성
```bash
mkdir libs
```

### 2. 브라우저에서 다운로드

**DxfParser:**
1. https://unpkg.com/dxf-parser@1.2.1/dist/dxf-parser.min.js
2. 우클릭 → 다른 이름으로 저장
3. `libs/dxf-parser.min.js` 로 저장

**JSZip:**
1. https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js
2. 우클릭 → 다른 이름으로 저장
3. `libs/jszip.min.js` 로 저장

### 3. Git 커밋
```bash
git add libs/
git commit -m "라이브러리 수동 추가"
git push
```

---

## 📦 파일 설명

### package.json
- npm 프로젝트 설정
- 의존성 라이브러리 목록
- 스크립트 명령어

### copy-libs.js
- node_modules에서 필요한 파일만 복사
- libs 폴더로 이동
- 자동화 스크립트

### .gitignore
- node_modules 제외 (용량 큼)
- libs 폴더는 포함 (GitHub Pages 필요)

### libs/ 폴더
- 실제 사용할 라이브러리 파일
- CDN 대체용
- ⚠️ 반드시 Git에 커밋!

---

## ✅ 체크리스트

설치 완료 확인:

- [ ] Node.js 설치됨
- [ ] `npm install` 실행
- [ ] `npm run copy-libs` 실행
- [ ] `libs/` 폴더 생성됨
- [ ] `libs/dxf-parser.min.js` 존재
- [ ] `libs/jszip.min.js` 존재
- [ ] Git 커밋 완료
- [ ] GitHub에 libs 폴더 확인됨
- [ ] 로컬 테스트 성공
- [ ] GitHub Pages 테스트 성공

---

## 🎉 완료!

이제 CDN 없이도 완벽하게 작동합니다!

**iPhone에서:**
1. Safari/Chrome 열기
2. URL 접속
3. 끝! 😊

**libs 폴더 덕분에:**
- ✅ 네트워크 문제 없음
- ✅ CDN 차단 문제 없음
- ✅ 빠른 로딩
- ✅ 안정적인 서비스

---

## 📞 추가 도움말

### 전체 명령어 요약:
```bash
# 1. 설치
npm install

# 2. 복사
npm run copy-libs

# 3. 커밋
git add libs/ package.json copy-libs.js .gitignore
git commit -m "라이브러리 로컬 설치"
git push origin main

# 4. 테스트 (로컬)
python -m http.server 8000
```

### 파일 확인:
```bash
# libs 폴더 내용 확인
ls -lh libs/

# 파일 크기 확인
du -h libs/*
```

**예상 결과:**
```
libs/dxf-parser.min.js  ~50KB
libs/jszip.min.js       ~100KB
```

---

**마지막 업데이트:** 2025-11-10  
**버전:** 1.0.0  
**문의:** GitHub Issues

